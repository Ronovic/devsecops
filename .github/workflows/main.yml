name: CI
on: [push]
jobs:
sast_scan:
name: bandit scan:
runs-on: ubuntu-latest

steps:
name: checkout code
uses: actions/checkout@v2

-name: checkout code
uses: actions/checkout@v2
with:
python-version: 3.8
-name: Install Bandit
run: pip install bandit

